# JabangStore

Open this folder in VS Code.

1. Install Node.js.
2. Run `npm install`.
3. Run `npm run dev`.
4. Open the local Vite address shown in the terminal.

The project is being built in phases according to the master PDF. The current files are Phase 1 foundation code; production backend functionality comes in later phases.
